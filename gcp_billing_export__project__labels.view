dimension: pk {
  primary_key: yes
  hidden: yes
  sql: FARM_FINGERPRINT(CONCAT(
    CAST(${TABLE}.usage_start_time AS STRING),
    ${TABLE}.sku.id,
    ${TABLE}.project.id,
    CAST(${TABLE}.export_time AS STRING),
    COALESCE(${TABLE}.usage.amount, 0)
  )) ;;
}
